Id: LogMessage

InputHandler:
  Type: "Gaucho.Handlers.InputHandler`1[[Gaucho.Server.Test.Controllers.LogMessage, Gaucho.Server.Test]], Gaucho"
#  Name: LogMessage
#  Filters:
#    - Message -> msg

OutputHandlers:
  - Type: Gaucho.Server.Test.Handlers.ConsoleOutputHandler, Gaucho.Server.Test
    Filters:
      - Message -> message
      - Level -> lvl
      - messAge -> error
#      - 'format <- [${lvl}] ${msg}'
#      - 'json <- [lvl,msg -> message]'

#  - Type: Gaucho.Test.LogQueueHandler, Gaucho.Test
#    Filters:
#      - Message -> msg
#      - Level -> lvl

